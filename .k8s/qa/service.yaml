apiVersion: v1
kind: Service

metadata:
  name: medication-suggest-ai-service
  namespace: qa

spec:
  selector:
    app: medication-suggest-ai

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer