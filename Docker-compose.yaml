version: "3.9"

networks:
  clinical-net:
    driver: bridge

volumes:
  ollama-data:

services:

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    networks:
      - clinical-net
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  ollama-pull:
    image: ollama/ollama:latest
    container_name: ollama-pull
    restart: "no"
    networks:
      - clinical-net
    volumes:
      - ollama-data:/root/.ollama
    entrypoint: ["/bin/sh","-c","sleep 10 && ollama pull medgemma:4b"]
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      ollama:
        condition: service_healthy

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medi-suggest-ai-backend
    restart: unless-stopped
    networks:
      - clinical-net
    environment:
      - OLLAMA_MODEL=medgemma:4b
      - OLLAMA_BASE_URL=http://ollama:11434
    ports:
      - "8000:8000"
    depends_on:
      ollama:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    container_name: medi-suggest-ai-nginx
    restart: unless-stopped
    networks:
      - clinical-net
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./frontend.html:/usr/share/nginx/html/index.html:ro
    depends_on:
      - backend