# Healthcare PoC — Python dependencies (Local LLM edition)
# Install: pip install -r requirements_healthcare.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0

# Local speech-to-text (replaces OpenAI Whisper API)
faster-whisper>=1.0.0

# No openai package needed — using local Ollama API