apiVersion: v1
kind: Service
metadata:
  name: medi-suggest-ai
  namespace: qa
  labels:
    app: medi-suggest-ai
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
  selector:
    app: medi-suggest-ai